name: metagenome-virus-host-paper-2023
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  # Historical software coordinate reported for the IMG/VR v3 application.
  # Pair with the Sept_2021_pub / iPHoP_db_Sept21 database; do not silently
  # substitute a newer database when reproducing the published benchmark.
  - iphop=1.0.0
